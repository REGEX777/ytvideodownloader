<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        #progress-bar {
            display: none;
            background-color: #3b82f6;
            height: 4px;
            width: 0;
            transition: width 0.4s ease;
        }
    </style>
</head>
<body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto mt-10">
        <div class="text-center mb-8">
            <h1 class="text-5xl font-extrabold text-blue-600"><%= title %></h1>
            <p class="text-lg text-gray-600 mt-4">Easily download YouTube videos in your preferred format with just a few clicks!</p>
        </div>
        <div class="w-full max-w-lg mx-auto bg-white shadow-lg rounded-lg p-8">
            <form action="/download" method="post" id="download-form">
                <div class="mb-6">
                    <label for="url" class="block text-lg font-medium text-gray-700">YouTube Video URL</label>
                    <input type="text" id="url" name="url" class="mt-2 block w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-md" placeholder="Paste the video link here" required>
                </div>
                <div class="mb-6">
                    <label for="quality" class="block text-lg font-medium text-gray-700">Select Quality</label>
                    <select id="quality" name="quality" class="mt-2 block w-full px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-md">
                        <option value="highest">Highest Quality (Audio & Video)</option>
                        <option value="lowest">Lowest Quality</option>
                        <option value="highestaudio">Highest Quality (Audio Only)</option>
                        <option value="lowestvideo">Lowest Quality (Video Only)</option>
                    </select>
                </div>
                <button type="submit" class="w-full bg-blue-600 text-white py-3 px-6 rounded-md hover:bg-blue-700 transition duration-300 text-lg font-medium">Download</button>
                <div id="progress-bar" class="mt-4 rounded-md"></div>
            </form>
        </div>
        <div class="text-center mt-8">
            <p class="text-gray-500 text-sm">ingo: The download process might take a few moments depending on the video size and your network speed.</p>
        </div>
    </div>
    <script>
        const form = document.getElementById('download-form');
        const progressBar = document.getElementById('progress-bar');

        form.addEventListener('submit', () => {
            progressBar.style.display = 'block';
            progressBar.style.width = '0';
            setTimeout(() => {
                progressBar.style.width = '100%';
            }, 100);
        });
    </script>
</body>
</html>
