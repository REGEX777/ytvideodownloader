<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= video.title %> - Video Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto mt-10">
        <h1 class="text-3xl font-bold text-gray-800 mb-6"><%= video.title %></h1>
        <div class="bg-white shadow-lg rounded-lg p-6">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Video Details</h2>
            <p class="text-lg text-gray-600 mb-4"><%= video.description %></p>
            <a href="<%= video.url %>" target="_blank" class="text-blue-600 hover:underline">Watch on YouTube</a>
            <p class="text-sm text-gray-500 mt-2">Published on <%= new Date(video.publishedAt).toLocaleDateString() %></p>
        </div>
    </div>
</body>
</html>
